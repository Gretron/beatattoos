import { BookProgress } from "../features/book/index";

/**
 * Book Page
 */
const Book = () => {
  return (
    <div className="book">
      <BookProgress />
    </div>
  );
};

export default Book;
